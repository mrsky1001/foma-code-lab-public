---
title: "DOCTYPE и структура HTML-документа"
highlight: html
type: theory
---

# DOCTYPE и структура HTML-документа

Каждый HTML-файл начинается с одной волшебной строки:

```html
<!DOCTYPE html>  <!-- объявление типа документа, не тег -->
```

Это **не тег** — это объявление. Оно говорит браузеру: «Эй, здесь HTML5!» Без него браузер включает режим совместимости со старыми сайтами 2000-х годов, и верстка может сломаться.

## Базовая структура любого HTML-файла

Вся страница состоит из четырёх обязательных частей:

| Тег | Роль |
|-----|------|
| `<!DOCTYPE html>` | Объявление типа документа |
| `<html lang="ru">` | Корневой элемент, атрибут lang — язык страницы |
| `<head>` | «Невидимая» голова: настройки, мета-теги, подключения |
| `<body>` | Всё что видит пользователь |

## Как вложить теги — пошагово

Теги в HTML вкладываются как матрёшки. Вот как строить структуру шаг за шагом:

**Шаг 1** — всегда начинаем с DOCTYPE:
```html
<!DOCTYPE html>
```

**Шаг 2** — открываем корневой тег `<html>`. В него войдёт абсолютно всё остальное:
```html
<!DOCTYPE html>
<html lang="ru">

</html>
```

**Шаг 3** — внутри `<html>` добавляем `<head>` (настройки) **и** `<body>` (контент). Важно: сначала head, потом body:
```html
<!DOCTYPE html>
<html lang="ru">
  <head>

  </head>
  <body>

  </body>
</html>
```

**Шаг 4** — заполняем `<head>`. Минимум — кодировка и заголовок вкладки:
```html
<head>
  <meta charset="UTF-8">          <!-- кодировка: поддержка кириллицы -->
  <title>Моя страница</title>     <!-- текст на вкладке браузера -->
</head>
```

**Шаг 5** — в `<body>` помещаем видимый контент:
```html
<body>
  <h1>Привет!</h1>
</body>
```

**Результат — полный скелет:**
```html
<!DOCTYPE html>                       <!-- объявление: это HTML5-документ -->
<html lang="ru">                      <!-- корневой тег, lang — язык страницы -->
  <head>                              <!-- невидимая "голова" страницы -->
    <meta charset="UTF-8">           <!-- кодировка: поддержка кириллицы -->
    <title>Моя страница</title>      <!-- текст во вкладке браузера -->
  </head>
  <body>                              <!-- всё видимое содержимое страницы -->
    <h1>Привет!</h1>                 <!-- главный заголовок страницы -->
  </body>
</html>                               <!-- закрывающий корневой тег -->
```

## Почему отступы важны

Обратите внимание: каждый вложенный тег сдвигается на **2 пробела**. Это не требование браузера — браузеру всё равно. Это требование к читаемости: по отступу сразу видно, какой тег куда вложен.

## Частые ошибки новичков

| Ошибка | Что произойдёт |
|--------|----------------|
| Пропустить `<!DOCTYPE html>` | Браузер включит «режим причуд» (quirks mode), верстка может ломаться |
| Закрыть `</html>` раньше `</body>` | Нарушение структуры, браузер исправит сам, но непредсказуемо |
| Положить `<body>` внутрь `<head>` | Страница не отобразится |
| Не закрыть тег | Все последующие теги окажутся «внутри» незакрытого |

## 🛠 Задание

Напишите полный скелет HTML-страницы: DOCTYPE, html, head, body. Внутри body добавьте заголовок `<h1>Привет!</h1>`.

Используйте пошаговый алгоритм выше — стройте структуру по одному тегу за раз!

```html:start
<!-- Начните с <!DOCTYPE html> -->
```

```html:solution
<!DOCTYPE html>                       <!-- объявление: это HTML5-документ -->
<html lang="ru">                      <!-- корневой тег, lang — язык страницы -->
  <head>                              <!-- невидимая "голова" страницы -->
    <meta charset="UTF-8">           <!-- кодировка: поддержка кириллицы -->
    <title>Моя страница</title>      <!-- текст во вкладке браузера -->
  </head>
  <body>                              <!-- всё видимое содержимое страницы -->
    <h1>Привет!</h1>                 <!-- главный заголовок страницы -->
  </body>
</html>                               <!-- закрывающий корневой тег -->
```
