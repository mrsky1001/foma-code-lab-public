---
title: "DOCTYPE и структура HTML-документа"
highlight: html
type: theory
---

# DOCTYPE и структура HTML-документа

Каждый HTML-файл начинается с одной волшебной строки:

```html
<!DOCTYPE html>  <!-- объявление типа документа, не тег -->
```

Это **не тег** — это объявление. Оно говорит браузеру: «Эй, здесь HTML5!» Без него браузер включает режим совместимости со старыми сайтами 2000-х годов, и верстка может сломаться.

## Базовая структура любого HTML-файла

Вся страница состоит из четырёх обязательных частей:

| Тег | Роль |
|-----|------|
| `<!DOCTYPE html>` | Объявление типа документа |
| `<html lang="ru">` | Корневой элемент, атрибут lang — язык страницы |
| `<head>` | «Невидимая» голова: настройки, мета-теги, подключения |
| `<body>` | Всё что видит пользователь |

## 🛠 Задание

Напишите полный скелет HTML-страницы: DOCTYPE, html, head, body. Внутри body добавьте заголовок `<h1>Привет!</h1>`.

```html:start
<!-- Начните с <!DOCTYPE html> -->
```

```html:solution
<!DOCTYPE html>          <!-- объявление: это HTML5-документ -->
<html lang="ru">         <!-- корневой тег, lang — язык страницы -->
<head>                   <!-- невидимая "голова" страницы -->
  <meta charset="UTF-8"> <!-- кодировка: поддержка кириллицы -->
  <title>Моя страница</title> <!-- текст во вкладке браузера -->
</head>
<body>                   <!-- всё видимое содержимое страницы -->
  <h1>Привет!</h1>       <!-- главный заголовок страницы -->
</body>
</html>                  <!-- закрывающий корневой тег -->
```
